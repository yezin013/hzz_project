# Content Service - Minimal Requirements for Lambda
# Only packages actually used by this service

# FastAPI & Server
fastapi==0.109.0
mangum==0.17.0

# MongoDB
motor==3.3.2
pymongo==4.5.0

# Pydantic
pydantic-settings==2.1.0

# HTTP & File Upload
httpx==0.27.0
python-multipart==0.0.9

# AWS (already in Lambda environment, but include for local dev)
boto3==1.34.0

# Minimal dependencies only
six
slowapi
